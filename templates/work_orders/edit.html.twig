{% extends 'base.html.twig' %}

{% block title %}Edit WorkOrders{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row mt-3">
            <div class="col-sm">
                <div class="card shadow mb-4">
                    <div class="card-header">
                        <h6 class="text-primary font-weight-bold">Werk bon bewerken</h6>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-sm-12">
                                {{ include('work_orders/_form.html.twig', {'statusEdit' : 'bezig'}) }}
                                <a href="{{ path('work_orders_index') }}" class="dropdown-item text-primary">Terug</a>
                                {{ include('work_orders/_delete_form.html.twig') }}
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
